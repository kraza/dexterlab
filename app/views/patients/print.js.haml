:plain
  $.dialog().open( "option", { title: "#{ escape_javascript( @patient.refrence_no ) }", width: "500px" } );
  $.dialog().content.html( "#{ escape_javascript( render(:partial => 'show', :locals => { :patient => @patient }) ) }" );
