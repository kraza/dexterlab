- if @patient
  :plain
    $.dialog().open( "option", {
      title: "#{ escape_javascript( @patient.refrence_no ) }"
    } ).buttons({
          "Ok":{
              'text':'Ok',
              'class': 'btn primary',
              'click': function(){
                  closeDialog();
              }}
        });

    // render model information with a to_html model method
    $.dialog().content.html(
      "#{ escape_javascript( render(:partial => 'show', :locals => { :patient => @patient }) ) }"
    );
- else
  :plain
    $.dialog().open_as_status( "option" {
      title: "No record found"
    } );

    $.dialog().content.html("<p>No record found.</p>");